
         Добавлено:

1. model->service->validator->CriteriaValidator
    * проверка на criteria == null
    * проверка на равно ли введенное имя класса для поиска null
    * в методе проверки коррекности аргументов, проверяется
    не равен ли полученный enum == null
    (может быть в том случае если введенно не корректное имя искомого класса
    и нужный enum не был найден)
   Данный класс полностью покрыт тестами
2. Кое где поменял названия переменных для большей удобночитаемости.

3.  model->dao->impl-> FileApplianceDAOImpl

 Сделал код более читаемым:   (разбил куски на методы)
        *  Поиск строк в файле и их валдиация совершается в отдельном методе
           и если он удачен, то тогда вернется список с возможными объектами
                            ( валидация ток по критерию поиска а не по валидности значений)

        * Создание объекта (объектов) и добавление их в финальный список в отдельном методе.
            Если все объекты будут не валидны, вернется исключение

